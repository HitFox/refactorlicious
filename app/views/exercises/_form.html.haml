%p Hey there buddy, thanks for wanting to contribute to our exponentionally growing platform.
= form_for exercise do |f|
  = render "shared/show_errors", exercise: exercise
  .category
    = f.label :exercise_category
    = f.collection_select(:exercise_category_id, ExerciseCategory.all, :id, :name)
  .points
    = f.label :points
    = f.select(:points, [10, 15, 25])
  .code_to_refactor
    = f.label :code_to_refactor
    = f.text_field :code_to_refactor
  .solution
    = f.label :solution
    = f.text_field :solution
  = f.submit "Submit"
